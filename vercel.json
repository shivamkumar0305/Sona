{
  "buildCommand": "corepack enable && corepack prepare pnpm@11.7.0 --activate && cd frontend && pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@11.7.0 --activate && cd frontend && pnpm install --frozen-lockfile",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
